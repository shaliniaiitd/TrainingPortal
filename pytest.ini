[pytest]
DJANGO_SETTINGS_MODULE = TrainingPortal.settings
python_files = tests.py test_*.py *_tests.py
testpaths = tests
asyncio_mode = off
#asyncio_mode = auto
; pythonpath = .
; ; asyncio_mode = auto
; ; asyncio_default_fixture_loop_scope = session
; DJANGO_SETTINGS_MODULE = TrainingPortal.settings
; python_files = tests.py test_*.py *_tests.py




;  [pytest]
; # Pytest configuration for TrainingPortal Tests
; # Central configuration for all test execution
;
; # Test discovery
; python_files = test_*.py *_test.py
; python_classes = Test*
; python_functions = test_*
;
; # Async support
; asyncio_mode = auto
;
; # Markers
; markers =
;     asyncio: marks tests as async
;     integration: marks tests as integration tests
;     unit: marks tests as unit tests
;     slow: marks tests as slow (deselect with '-m "not slow"')
;     critical: marks tests as critical path
;     smoke: marks tests as smoke tests
;     regression: marks tests as regression tests
;     api: marks tests as API tests
;     ui: marks tests as UI tests
;     data: marks tests as data tests
;
; # Test output
; console_output_style = progress
; addopts =
;     -v
;     --strict-markers
;     --tb=short
;     --capture=no
;     --asyncio-mode=auto
;     --timeout=300
;
; # Logging
; log_cli = true
; log_cli_level = INFO
; log_cli_format = %(asctime)s [%(levelname)8s] %(message)s
; log_cli_date_format = %Y-%m-%d %H:%M:%S
;
; log_file = test_execution.log
; log_file_level = DEBUG
; log_file_format = %(asctime)s [%(levelname)8s] %(filename)s:%(lineno)d - %(message)s
;
; # Timeout
; timeout = 300
; timeout_method = thread
;
; # Parallel execution (pytest-xdist)
; # Use: pytest -n auto  or  pytest -n 4
; # Note: Playwright's native workers are handled via context/browser scope
;
; # Allure reporting
; # Generate: pytest --alluredir=allure-results
; # View: allure serve allure-results
;
; # Minimum Python version
; minversion = 7.0
;
; # Test paths
; testpaths = tests
;
; # xfail behavior
; xfail_strict = false
